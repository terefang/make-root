<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>parent</artifactId>
        <groupId>terefang.makeroot</groupId>
        <version>2021.1.0</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <groupId>terefang.makeroot.muslsys</groupId>
    <artifactId>proftpd</artifactId>
    <version>${version.proftpd}</version>
    <packaging>pom</packaging>

    <properties>
        <prg.archive>${project.artifactId}-${project.version}.tar.gz</prg.archive>
        <prg.tag>${project.artifactId}-${project.version}</prg.tag>
        <prg.url>ftp://ftp.proftpd.org/distrib/source/proftpd-${project.version}.tar.gz</prg.url>

        <timestamp>${maven.build.timestamp}</timestamp>
        <maven.build.timestamp.format>yyyyMMddHHmm</maven.build.timestamp.format>
    </properties>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>1.8</version>
                <inherited>false</inherited>
                <dependencies>
                    <dependency>
                        <groupId>org.redline-rpm</groupId>
                        <artifactId>redline</artifactId>
                        <version>1.2.9</version>
                        <classifier>jar-with-dependencies</classifier>
                    </dependency>
                    <dependency>
                        <groupId>org.apache.ant</groupId>
                        <artifactId>ant-compress</artifactId>
                        <version>1.5</version>
                    </dependency>
                    <dependency>
                        <groupId>com.github.terefang</groupId>
                        <artifactId>ant-archive-plus</artifactId>
                        <version>1.5.2</version>
                    </dependency>
                </dependencies>
                <executions>
                    <execution>
                        <id>ant</id>
                        <phase>package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target xmlns:if="ant:if" xmlns:unless="ant:unless">
                                <taskdef classname="org.redline_rpm.ant.RedlineTask" name="xrpm" />
                                <taskdef classname="org.apache.ant.compress.taskdefs.Cpio" name="cpio" />

                                <mkdir dir="${root.dir}"/>
                                <mkdir dir="${toolchain.dir}"/>
                                <mkdir dir="${project.basedir}/src/main/dist"/>
                                <mkdir dir="${project.build.directory}/${project.artifactId}/${prg.tag}"/>
                                <mkdir dir="${resources.target}"/>

                                <condition property="archive.exists">
                                    <resourceexists>
                                        <file file="${project.basedir}/src/main/dist/${prg.archive}"/>
                                    </resourceexists>
                                </condition>

                                <exec unless:true="${final.exists}" dir="${toolchain.dir}" executable="tar">
                                    <arg line="-xvf ${resources.target}/gcc-10-x86_64-linux-musl-native.tar.gz" />
                                </exec>
                                <exec unless:true="${final.exists}" dir="${toolchain.dir}" executable="tar">
                                    <arg line="-xvf ${resources.target}/muslsys-musl-bsdcompat-0.1.tgz" />
                                </exec>

                                <exec unless:true="${final.exists}" dir="${toolchain.dir}" executable="tar">
                                    <arg line="-xvf ${resources.target}/muslsys-zlib-${version.zlib}.tgz" />
                                </exec>
                                <exec unless:true="${final.exists}" dir="${toolchain.dir}" executable="tar">
                                    <arg line="-xvf ${resources.target}/muslsys-sqlite-${version.sqlite}.tgz" />
                                </exec>
                                <exec unless:true="${final.exists}" dir="${toolchain.dir}" executable="tar">
                                    <arg line="-xvf ${resources.target}/muslsys-libressl-${version.libressl}-lib.tgz" />
                                </exec>
                                <exec unless:true="${final.exists}" dir="${toolchain.dir}" executable="tar">
                                    <arg line="-xvf ${resources.target}/muslsys-pcre-${version.pcre}.tgz" />
                                </exec>
                                <exec unless:true="${final.exists}" dir="${toolchain.dir}" executable="tar">
                                    <arg line="-xvf ${resources.target}/muslsys-libxcrypt-${version.libxcrypt}.tgz" />
                                </exec>
                                <exec unless:true="${final.exists}" dir="${toolchain.dir}" executable="tar">
                                    <arg line="-xvf ${resources.target}/muslsys-openldap-${version.openldap}.tgz" />
                                </exec>
                                <exec unless:true="${final.exists}" dir="${toolchain.dir}" executable="tar">
                                    <arg line="-xvf ${resources.target}/muslsys-libsodium-${version.libsodium}.tgz" />
                                </exec>

                                <delete unless:true="${final.exists}">
                                    <fileset dir="${toolchain.dir}" includes="usr/lib/*.la"/>
                                    <fileset dir="${toolchain.dir}" includes="usr/lib/*.so*"/>
                                    <fileset dir="${toolchain.dir}" includes="usr/lib/pkgconfig/*"/>
                                </delete>

                                <get unless:true="${archive.exists}" src="${prg.url}" dest="${project.basedir}/src/main/dist/${prg.archive}"/>

                                <exec unless:true="${final.exists}" dir="${project.build.directory}/${project.artifactId}/${prg.tag}" executable="tar">
                                    <arg line="-xvf ${project.basedir}/src/main/dist/${prg.archive} --strip 1" />
                                </exec>

                                <exec unless:true="${final.exists}" dir="${project.build.directory}/${project.artifactId}/${prg.tag}" executable="configure" resolveexecutable="true">
                                    <env key="PATH" value="${toolchain.dir}/bin:${env.PATH}" />
                                    <env key="CC" value="x86_64-linux-musl-gcc --static" />
                                    <env key="CXX" value="x86_64-linux-musl-g++ --static" />
                                    <env key="CFLAGS" value="--static -I ${toolchain.dir}/include" />
                                    <env key="LDFLAGS" value="-static -L${toolchain.dir}/lib " />
                                    <arg value="--prefix=/usr/proftpd" />
                                    <arg value="--exec-prefix=/usr/proftpd" />
                                    <arg value="--sysconfdir=/etc/proftpd" />
                                    <arg value="--sharedstatedir=/var/run" />
                                    <arg value="--localstatedir=/var/proftpd" />
                                    <arg value="--enable-autoshadow" />
                                    <arg value="--enable-static" />
                                    <arg value="--disable-shared" />
                                    <arg value="--disable-curses" />
                                    <arg value="--disable-dso" />
                                    <arg value="--disable-nls" />
                                    <arg value="--disable-ncurses" />
                                    <arg value="--enable-pcre" />
                                    <arg value="--enable-openssl" />
                                    <arg value="--enable-sodium" />
                                    <arg value="--with-pic" />
                                    <arg value="--with-includes=${toolchain.dir}/include" />
                                    <arg value="--with-libraries=${toolchain.dir}/lib" />
                                    <arg value="--with-modules=mod_ldap:mod_tls:mod_sftp:mod_sftp_sql:mod_exec:mod_rewrite:mod_ifsession:mod_digest:mod_radius:mod_unique_id:mod_wrap2:mod_wrap2_sql:mod_wrap2_file:mod_sql:mod_sql_sqlite:mod_sql_passwd" />
                                </exec>
                                <exec unless:true="${final.exists}" dir="${project.build.directory}/${project.artifactId}/${prg.tag}" executable="make">
                                    <env key="PATH" value="${toolchain.dir}/bin:${env.PATH}" />
                                    <env key="CC" value="x86_64-linux-musl-gcc --static" />
                                    <env key="CXX" value="x86_64-linux-musl-g++ --static" />
                                    <env key="CFLAGS" value="--static -I ${toolchain.dir}/include " />
                                    <env key="LDFLAGS" value="-static -L${toolchain.dir}/lib " />
                                    <arg line="-j 1" />
                                </exec>
                                <exec unless:true="${final.exists}" dir="${project.build.directory}/${project.artifactId}/${prg.tag}" executable="make">
                                    <env key="PATH" value="${env.PATH}:${toolchain.dir}/bin" />
                                    <arg line="-j 1 DESTDIR=${root.dir} install" />
                                </exec>

                                <xrpm
                                        architecture="X86_64"
                                        destination="${project.build.directory}"
                                        license="ASL"
                                        group="base"
                                        name="toolbox-${project.artifactId}"
                                        summary="${project.artifactId}."
                                        distribution="nos"
                                        packager="nos"
                                        url="none"
                                        vendor="nos"
                                        epoch="${buildNumber}"
                                        release="${buildNumber}.${timestamp}"
                                        version="${project.version}" >

                                    <rpmfileset prefix="/etc/${project.artifactId}" filemode='640' dirmode="750" dir="${root.dir}/etc/${project.artifactId}" username="${project.artifactId}" group="${project.artifactId}" addParents="false" config="true" />

                                    <rpmfileset prefix="/usr/${project.artifactId}/bin" filemode='755' dirmode="755" file="${project.build.directory}/${project.artifactId}/${prg.tag}/proftpd" username="${project.artifactId}" group="${project.artifactId}" addParents="false" />

                                    <emptydir path="/etc/${project.artifactId}/conf.d" />
                                    <emptydir path="/var/log/${project.artifactId}" />
                                    <emptydir path="/var/${project.artifactId}" />

                                </xrpm>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>