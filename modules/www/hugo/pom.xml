<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>parent</artifactId>
        <groupId>terefang.makeroot</groupId>
        <version>2021.1.0</version>
        <relativePath>../../../pom.xml</relativePath>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <groupId>terefang.makeroot.baseos</groupId>
    <artifactId>hugo</artifactId>
    <version>${version.hugo}</version>
    <packaging>pom</packaging>

    <properties>
        <prg.tag>${project.artifactId}-${project.version}</prg.tag>
        <prg.archive>${prg.tag}.tar.gz</prg.archive>
        <prg.url1>https://github.com/gohugoio/hugo/releases/download/v${project.version}/hugo_${project.version}_linux-amd64.tar.gz</prg.url1>
        <prg.url2>https://github.com/gohugoio/hugo/releases/download/v${project.version}/hugo_extended_${project.version}_linux-amd64.tar.gz</prg.url2>
        <prg.url3>https://github.com/gohugoio/hugo/releases/download/v${project.version}/hugo_extended_withdeploy_${project.version}_linux-amd64.tar.gz</prg.url3>
    </properties>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>1.8</version>
                <inherited>false</inherited>
                <dependencies>
                    <dependency>
                        <groupId>org.redline-rpm</groupId>
                        <artifactId>redline</artifactId>
                        <version>1.2.9</version>
                        <classifier>jar-with-dependencies</classifier>
                    </dependency>
                    <dependency>
                        <groupId>org.apache.ant</groupId>
                        <artifactId>ant-compress</artifactId>
                        <version>1.5</version>
                    </dependency>
                    <dependency>
                        <groupId>com.github.terefang</groupId>
                        <artifactId>ant-archive-plus</artifactId>
                        <version>1.5.2</version>
                    </dependency>
                </dependencies>
                <executions>
                    <execution>
                        <id>ant</id>
                        <phase>package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target xmlns:if="ant:if" xmlns:unless="ant:unless">
                                <taskdef classname="org.redline_rpm.ant.RedlineTask" name="xrpm" />
                                <taskdef classname="org.apache.ant.compress.taskdefs.Cpio" name="cpio" />

                                <taskdef name="xtar" classname="com.github.terefang.ant.archiveplus.taskdefs.Tar" />
                                <typedef name="symlink" classname="com.github.terefang.ant.archiveplus.resources.SymbolicLink" />

                                <mkdir dir="${root.dir}/usr/bin"/>
                                <mkdir dir="${rpm.path}"/>
                                <mkdir dir="${project.build.directory}/${project.artifactId}"/>
                                <mkdir dir="${toolchain.dir}"/>
                                <mkdir dir="${project.basedir}/src/main/dist"/>

                                <get src="${prg.url1}" dest="${project.basedir}/src/main/dist/1${prg.archive}" />
                                <get src="${prg.url2}" dest="${project.basedir}/src/main/dist/2${prg.archive}" />
                                <get src="${prg.url3}" dest="${project.basedir}/src/main/dist/3${prg.archive}" />

                                <exec dir="${root.dir}" executable="tar">
                                    <arg line="-xvf ${project.basedir}/src/main/dist/1${prg.archive}" />
                                </exec>
                                <exec dir="${root.dir}" executable="tar">
                                    <arg line="-xvf ${project.basedir}/src/main/dist/2${prg.archive}" />
                                </exec>
                                <exec dir="${root.dir}" executable="tar">
                                    <arg line="-xvf ${project.basedir}/src/main/dist/3${prg.archive}" />
                                </exec>
                                <exec dir="${root.dir}" executable="cp">
                                    <arg line="-v ${project.artifactId} ${root.dir}/usr/bin/" />
                                </exec>

                                <xrpm
                                        architecture="X86_64"
                                        destination="${project.build.directory}"
                                        license="ASL"
                                        group="${project.artifactId}"
                                        name="toolbox-${project.artifactId}"
                                        summary="${project.artifactId}"
                                        distribution="${project.artifactId}"
                                        packager="${project.artifactId}"
                                        url=""
                                        vendor="${project.artifactId}"
                                        epoch="${buildNumber}"
                                        release="${buildNumber}~${timestamp}"
                                        version="${project.version}">

                                    <rpmfileset prefix="/data/toolbox/xbin" filemode='555' dir="${root.dir}/usr/bin" username="root" group="root" addParents="false" />

                                </xrpm>
                                <copy file="${project.build.directory}/toolbox-${project.artifactId}-${project.version}-${buildNumber}~${timestamp}.x86_64.rpm" todir="${toolbox.dir}/"/>

                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>